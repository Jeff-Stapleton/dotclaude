{
  "name": "figma",
  "version": "1.0.0",
  "description": "Interface with Figma API to read files, extract design tokens, and export assets",
  "author": "Claude Code",
  "permissions": [
    "network:https://api.figma.com/*"
  ],
  "environment": {
    "required": [
      "FIGMA_API_TOKEN"
    ]
  },
  "commands": {
    "get_file_structure": {
      "description": "Get the structure of a Figma file including pages and frames",
      "args": ["file_key"]
    },
    "extract_design_tokens": {
      "description": "Extract colors, typography, and spacing tokens from a file",
      "args": ["file_key"]
    },
    "export_assets": {
      "description": "Export assets as images from specific nodes",
      "args": ["file_key", "node_ids", "format?", "scale?"]
    },
    "analyze_components": {
      "description": "Analyze component structure and organization",
      "args": ["file_key"]
    }
  }
}
